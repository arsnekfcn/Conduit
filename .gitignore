# build output
bin/
obj/
*.user

# IDE / editor
.vs/
.vscode/
.idea/

# local build override (points SeBin64 at this machine's SE install)
Directory.Build.props

# local dev convenience (machine-specific deploy path)
deploy.sh

# runtime output (offline batches, dry-run, diagnostics, subtype census)
offline/
dryrun/
diag/
census/

# No committed binary deps
libs/
